{
  "name": "baking-tools",
  "version": "0.3.2",
  "description": "Convert cups to grams for several common baking ingredients",
  "main": "elm.js",
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2",
    "elm-live": "^4.0.1",
    "serve": "^11.2.0"
  },
  "scripts": {
    "start": "elm-live src/Main.elm --start-page=index.html --open -- --output=elm-dist.js --debug",
    "build": "elm make src/Main.elm --optimize --output=elm-dist.js",
    "serve": "npm run build && serve .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
